# backend

Python 标准库实现的第一版 Demo 后端，本地内存保存生成结果。

启动：

```powershell
cd E:\agent\七牛云\backend
py server.py
```

接口：

- `GET /api/health`
- `POST /api/analyze`
- `POST /api/generate`
- `POST /api/validate`

